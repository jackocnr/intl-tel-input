// Site-wide Bootstrap entry. Importing each component registers its
// data-API listeners so `data-bs-toggle="collapse|dropdown|offcanvas"`
// markup throughout the site is wired up automatically.
// Tooltip and Toast are also imported here so they're available for
// programmatic use without each call site needing to register them.
import "bootstrap/js/dist/collapse";
import "bootstrap/js/dist/dropdown";
import "bootstrap/js/dist/offcanvas";
import "bootstrap/js/dist/tooltip";
import "bootstrap/js/dist/toast";
